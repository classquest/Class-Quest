var mysql=require('mysql');
console.log(mysql);
var connection 
var mysql = require('mysql');
console.log(mysql);
var connection = mysql.createConnetion({
  host: 'localhost',
    user: 'wp2016_groupI',
      password: '********',
        database: 'wp2016_group'
        });
          
          connection.connect();
